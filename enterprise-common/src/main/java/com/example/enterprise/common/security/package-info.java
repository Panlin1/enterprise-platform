/**
 * Security-related helpers shared across services.
 * <p>
 * Sa-Token / Spring Security specific configuration lives in auth / gateway modules.
 * This package may hold password encoder helpers, permission string constants, etc. in later phases.
 */
package com.example.enterprise.common.security;