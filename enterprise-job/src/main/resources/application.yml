server:
  port: 8086

spring:
  application:
    name: enterprise-job

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
