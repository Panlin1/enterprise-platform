server:
  port: 8081

spring:
  application:
    name: enterprise-auth

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
